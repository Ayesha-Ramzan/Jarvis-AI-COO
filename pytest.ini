[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
env =
    ENVIRONMENT=test
    DATABASE_URL=sqlite+aiosqlite://
    SEED_FIXTURE_ORGANIZATIONS=false
    RATE_LIMIT_PER_MINUTE=0
    AUTH_SIGNING_KEY=test-only-signing-key-not-a-secret
